<p>Welcome <%= @resource.name %>!</p>
<% if @resource.role == "student" %>
    <p>You can confirm your account email through the link below:</p>
    <p><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token) %></p>
<% elsif @resource.role == "organiser" %>
    <p>CCSGP admin created an account for you! You can confirm your account email through the link below:</p>
    <p>Your password is: <%= @resource.password %></p>
    <p>Please update your password immediately after signing in!</p>
    <p><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token) %></p>
<% end %>

